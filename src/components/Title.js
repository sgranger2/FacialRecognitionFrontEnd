import React from 'react';

const Title = ({ isSignedIn, onRouteChange }) => {
    return (
        <div >
           <h1>Shane's React Demographics Facial Recognition App</h1>
        </div>
    );
}

export default Title;